# AGENTS.md

## 1. 适用范围与核心原则

本文件用于约束本项目中的代码修改、调试、测试、版本管理、Windows Electron 打包以及相关工具调用行为。

目标是：

- 正确完成用户当前任务；
- 尽量减少无意义的模型调用和工具调用；
- 避免过度工程化；
- 保护用户已有代码、程序、文件和构建产物；
- 对构建、打包和排错采用最短、最稳定的标准流程。

### 核心原则

1. **最小必要操作**

   优先使用最短、最直接、对现有工程影响最小的方式完成用户目标。

2. **严格遵守当前任务范围**

   不要因为发现额外问题、存在可优化空间或“顺手可以做”，就自动扩大任务。

3. **先定位根因，再采取 workaround**

   单个局部错误不应立即升级为构建系统改造、目录重构、备用发布体系或复杂恢复方案。

4. **保持现有工程结构**

   除非用户明确要求，或已确认现有结构本身必须修改，否则不要主动重构：

   - 项目目录；
   - 构建链；
   - 打包链；
   - 发布方式；
   - Electron 配置；
   - Git 工作流。

5. **避免过度工程化**

   以下任务原则上应保持简单：

   - 修改版本号；
   - 修改局部功能；
   - 修复局部 bug；
   - 构建；
   - 打包；
   - 验证 EXE；
   - 查看产物；
   - 修复明确的环境问题。

6. **成功即停止**

   达到用户明确要求的完成条件后立即停止。

   不要因为：

   > “还可以进一步检查”

   就自动理解为：

   > “必须继续检查”。

7. **保护用户环境**

   不得随意：

   - 关闭用户原本运行的程序；
   - 删除用户文件；
   - 覆盖已有构建产物；
   - 清理整个 release；
   - 丢弃 Git 修改；
   - 重置用户工作区。

---

## 2. 任务范围与打包触发条件

### 2.1 普通开发任务

当用户要求：

- 修改功能；
- 修复 bug；
- 修改 UI；
- 调整数据处理；
- 优化局部逻辑；
- 分析问题；
- 调试代码；

默认任务范围仅包括：

```text
定位问题
→ 完成必要修改
→ 执行必要验证
→ 报告结果
→ 停止
```

### 2.2 不得自动进入发布流程

**除非用户当前明确要求打包、发布、生成正式 EXE 或完成正式版本，否则普通功能修改完成后不得自动进入：**

- 版本号升级；
- Windows EXE 打包；
- release 发布；
- Git tag；
- hash 计算；
- 发布产物归档；
- 与发布相关的额外 smoke test。

不得仅因为：

> “完成了一个新功能”

就自行认为：

> “必须发布一个新版本”。

### 2.3 可以进入打包流程的情况

仅在以下情况下执行正式 Windows 打包：

1. 用户明确要求：

   - “打包”
   - “生成 EXE”
   - “发布”
   - “打一个新版本”
   - “生成正式版本”

2. 用户当前任务明确包含发布步骤。

3. 用户明确要求完成完整 release 流程。

---

## 3. 当前状态检查原则

根据任务需要检查当前真实工作区状态，但不要无目标扫描整个项目。

可能需要检查：

- 当前目标文件；
- 当前 workspace；
- 当前 branch；
- `git status --short`；
- `git diff --stat`；
- 与当前修改直接相关的 diff；
- 当前版本号；
- 必要的项目配置。

始终记住：

> Git HEAD 不等于当前完整工作区状态。

但是：

**不要为了“确认一切”而在每个小步骤后重复执行相同的 Git/status 检查。**

如果状态没有变化，则不要重复获取完全相同的信息。

---

## 4. Agent 自有状态管理

Codex 必须持续记录当前任务中由自己启动或创建的运行状态，包括但不限于：

- Vite dev server；
- Electron 开发实例；
- Electron 测试实例；
- Node watcher；
- 浏览器自动化测试；
- 临时 HTTP 服务；
- preview server；
- 临时构建进程；
- 当前任务创建的临时目录；
- 当前任务创建的临时文件。

### 规则

1. 启动长期运行进程时，应记录：

   - 用途；
   - 启动命令；
   - PID 或其他可识别信息。

2. 从：

   ```text
   开发 / 调试 / UI 验证
   ```

   切换到：

   ```text
   正式构建 / 打包 / 发布
   ```

   前，先清理由**当前任务自行启动且已经不再需要**的：

   - Vite；
   - Electron；
   - watcher；
   - preview；
   - 自动化测试；
   - 临时服务。

3. **不要默认关闭用户原本正在运行的正式版本程序。**

4. 如果必须关闭用户程序，应先说明原因并征得用户同意。

5. 遇到以下问题：

   - 端口占用；
   - 文件锁；
   - `EPERM`；
   - `EBUSY`；
   - rename 失败；
   - delete 失败；
   - Access denied；

   第一优先级是检查：

   > 当前任务中 Codex 自己启动的进程。

   不要首先怀疑用户程序。

---

## 5. 代码修改策略

1. 修改前先定位与需求直接相关的：

   - 文件；
   - 模块；
   - 调用链；
   - 数据结构。

2. 不做无目标的全项目扫描。

3. 优先局部修改。

4. 除非必要，不修改：

   - 无关模块；
   - 无关命名；
   - 无关 UI；
   - 无关配置；
   - 无关依赖；
   - 无关架构。

5. 不因为“顺手优化”自动扩大改动范围。

6. 发现范围外的问题时：

   - 可以在最终说明中指出；
   - 默认不要自动修复。

7. 对复杂修改，在关键阶段进行必要验证。

8. 如果同一个检查已经通过，并且之后没有相关代码变化，不要重复执行。

9. 若连续两次尝试仍没有解决同一个异常：

   - 停止无序试错；
   - 重新确认根因；
   - 总结当前已知状态；
   - 选择下一步最小风险方案。

---

## 6. 工具调用与额度效率原则

### 6.1 减少无意义的模型—工具往返

能够一次完成的操作，不要人为拆成多个极小步骤。

例如能够一次执行：

```powershell
git status --short
git diff --stat
```

时，不需要为了获取两个简单结果分别进行多轮模型决策。

对于多个安全、独立、只读检查，如果可以合理合并，应优先合并执行。

### 6.2 禁止短间隔反复轮询

对于：

- build；
- test；
- package；
- electron-builder；
- npm install；
- 其他预期需要一定时间的命令；

应给予合理的单次等待时间。

**不要以 1～5 秒等短间隔反复执行：**

```text
poll
→ poll
→ poll
→ poll
```

只有在：

- 命令确实仍然后台运行；
- 当前工具无法等待完成；
- 必须确认最终状态；

时才进行再次查询。

### 6.3 避免机械步骤触发大量工具调用

以下操作不得被无意义拆分成大量独立步骤：

- 查看版本；
- 查看产物；
- 查询文件存在；
- 查询构建结果；
- 获取文件大小；
- 读取 Git 状态；
- 查看测试是否结束；
- 查看 package 是否完成。

### 6.4 不重复验证没有变化的状态

如果：

- 代码没有变化；
- 构建产物没有变化；
- 配置没有变化；
- 已确认的状态没有变化；

则不要重复执行完全相同的验证。

### 6.5 不做无意义的发布附加检查

除非用户明确要求，不要自动增加：

- SHA-256；
- 多次文件大小检查；
- 多次版本检查；
- 多轮启动测试；
- 多轮 Git status；
- 额外 release 目录审计；
- 对同一 EXE 的重复检查。

---

## 7. 测试与验证原则

### 7.1 常规代码修改

根据实际修改范围运行必要测试。

不要默认执行与当前修改无关的完整测试矩阵。

### 7.2 已通过的测试不要重复执行

如果同一份代码在当前任务中：

- 已执行相同测试；
- 测试通过；
- 之后相关代码没有变化；

则不得为了形式完整再次运行相同测试。

### 7.3 正式打包前

如果当前任务明确要求正式发布，原则上应确保必要测试已经通过。

本项目可执行：

```powershell
npm.cmd test
```

是否需要单独执行：

```powershell
npx.cmd tsc -b
```

根据当前状态决定。

如果：

```powershell
npm.cmd run package:win
```

本身已经包含 TypeScript 构建，并且没有必要提前单独检查，则不需要为了形式完整重复执行 `tsc -b`。

### 7.4 验证边界

只执行能够证明当前任务完成所必需的验证。

除非用户明确要求，不自动进行：

- 全项目代码审计；
- 大范围性能测试；
- 长时间压力测试；
- 无关浏览器测试；
- 无关 Electron 测试；
- 无关 Git 清理；
- 重复启动相同程序；
- 重复打包；
- 多套替代构建验证。

---

## 8. 版本管理规则

1. **不要自行猜测新版本号。**

2. 用户明确指定版本号时，使用：

```powershell
npm.cmd version <新版本号> --no-git-tag-version
```

例如：

```powershell
npm.cmd version 0.4.6 --no-git-tag-version
```

3. 不创建 Git tag，除非用户明确要求。

4. 如果用户只要求：

> “重新打包当前版本”

则不得自动递增版本号。

5. 用户明确表示：

> “当前新版本完成”

但没有提供版本号时：

   - 先读取当前版本；
   - 检查当前上下文是否已经能够确定目标版本；
   - 能确定则使用已确定版本；
   - 确实无法确定时再询问用户。

6. 不要因为某个功能修改完成就自动执行 `npm version`。

---

## 9. Windows Electron 标准打包流程

本项目 Windows 正式打包的标准命令为：

```powershell
npm.cmd run package:win
```

该脚本负责项目既定的 Windows 构建流程，包括当前项目配置中的：

- TypeScript；
- Vite；
- 图标；
- Electron；
- electron-builder；
- Windows portable EXE。

### 正式打包前

按以下顺序处理：

1. 确认用户确实要求打包；
2. 确认目标版本号；
3. 确认必要测试已经通过；
4. 不重复运行已经通过且代码未变化的测试；
5. 停止当前任务中 Codex 自己启动且已不再需要的：

   - Vite；
   - Electron；
   - watcher；
   - preview；
   - 自动化测试服务；

6. 不关闭用户原本运行的正式版本；
7. 执行：

```powershell
npm.cmd run package:win
```

### 打包过程中

给予命令足够的等待时间。

不要因为短时间内没有返回就频繁轮询。

### 打包成功条件

确认目标产物存在：

```text
release/旅行轨迹记录与规划工具 <版本号>.exe
```

默认满足以下条件即认为正式打包完成：

```text
package:win 成功
+ 目标 EXE 存在
+ EXE 版本正确
+ 没有新的阻塞性构建错误
= 完成
```

达到以上条件后立即停止。

---

## 10. `EPERM` / 文件占用 / `win-unpacked` 故障处理

当 electron-builder 在以下位置或类似步骤发生：

- `release/win-unpacked`；
- `win-unpacked.tmp`；
- rename 失败；
- delete 失败；
- `EPERM`；
- `EBUSY`；
- Access denied；

必须按照以下顺序处理。

### 第一级：检查 Codex 自己造成的占用

优先检查当前任务是否启动过：

- Vite dev server；
- Electron 开发实例；
- Node watcher；
- preview server；
- 浏览器自动化；
- 测试服务；
- 其他监视当前项目目录的进程。

如果存在：

> 由 Codex 自己启动且当前已经不再需要的进程

则先停止。

### 第二级：检查目标文件或目录的明确占用

只针对：

- `release`；
- `win-unpacked`；
- `win-unpacked.tmp`；
- 具体错误文件；

定位可能的占用。

不得无差别：

- kill 所有 Node；
- kill 所有 Electron；
- kill 用户程序；
- kill 系统进程。

### 第三级：清理本次失败构建产生的临时状态

只清理：

> 本次失败构建明确生成，并且确认安全删除的临时文件或目录。

不得默认删除：

```text
整个 release
```

不得删除用户已有 EXE。

### 第四级：标准流程只自动重试一次

针对已确认根因完成最小处理后，可以重新执行一次：

```powershell
npm.cmd run package:win
```

### 重试上限

**同一次打包任务中，如果第二次标准打包仍然出现相同或实质相同的错误：**

立即停止自动重试。

不得继续：

```text
第三次 package
第四次 package
第五次 package
……
```

此时应：

1. 汇总当前错误；
2. 说明已确认的根因；
3. 说明已经采取的处理；
4. 说明两次标准打包结果；
5. 向用户报告当前阻塞点。

---

## 11. 禁止过早采用复杂 workaround

在标准打包流程尚未被证明无法恢复之前，不得为了单次：

- `EPERM`；
- 文件锁；
- rename 失败；
- temporary directory 错误；

自行采取以下方案：

- 创建第二套正式 release 目录；
- 创建备用 release 体系；
- 更改正式输出目录；
- 手工复制 Electron runtime；
- 手工拼装 Electron 应用；
- 手工构建 portable 包；
- 绕过 electron-builder 正常流程；
- 重写打包脚本；
- 改变 Electron 架构；
- 大范围修改 package.json；
- 建立长期 workaround。

如果标准流程第二次仍失败，并且确实考虑复杂 workaround，必须先向用户说明：

1. 已确认根因；
2. 标准流程为什么无法恢复；
3. workaround 的具体方案；
4. 对现有项目的影响；
5. 可能风险。

然后等待用户明确授权。

---

## 12. PowerShell 与 Windows 环境问题

本项目默认运行环境为 Windows。

Shell 操作优先使用 PowerShell。

npm 命令优先使用：

```powershell
npm.cmd
```

而不是假设 Bash、WSL 或 GNU 工具存在。

### `powershell.exe ENOENT`

如果 electron-builder 报：

```text
spawn powershell.exe ENOENT
```

首先检查：

```powershell
Get-Command powershell.exe -ErrorAction SilentlyContinue
```

并检查：

```powershell
$env:PATH
```

确认确实属于 PATH 问题后，可在当前 PowerShell 会话中执行：

```powershell
$env:PATH = "C:\Windows\System32\WindowsPowerShell\v1.0;" + $env:PATH
```

然后重新执行标准打包流程。

不要因为一个 PATH 环境问题：

- 修改构建架构；
- 修改 Electron 打包体系；
- 重写项目脚本；
- 创建备用打包流程。

---

## 13. Git 与文件安全

1. 不执行破坏性 Git 操作，除非用户明确要求。

2. 不擅自执行：

```text
git reset --hard
git clean
强制 checkout 覆盖
强制丢弃修改
```

3. 不为了得到“干净工作区”删除用户已有修改。

4. 不覆盖与当前任务无关的文件。

5. 不删除与当前任务无关的未跟踪文件。

6. 当前任务结束时没有必要为了形式整洁继续进行额外 Git 操作。

7. 如果发现非当前任务产生的修改：

   - 保留；
   - 避免覆盖；
   - 必要时报告用户。

---

## 14. 任务完成条件

### 普通代码修改任务

默认 Definition of Done：

```text
用户要求的功能已经实现
+ 与修改直接相关的必要验证通过
= 完成
```

完成后：

- 简洁说明修改内容；
- 简洁说明测试结果；
- 如有尚未验证项目，明确说明；
- 停止。

**不得自动进入打包。**

### 打包 EXE 任务

默认 Definition of Done：

```text
必要测试已经通过
+ 标准 package:win 成功
+ 目标 EXE 存在
+ 版本正确
= 完成
```

完成后只需要报告：

- 版本号；
- EXE 路径或文件名；
- 打包成功状态；
- 必要的非阻塞警告。

然后立即停止。

除非用户明确要求，不继续：

- hash；
- 多次 smoke test；
- 多轮 status；
- release 审计；
- 备用打包；
- 额外代码优化；
- Git 清理。

---

## 15. 异常任务的停止条件

如果任务出现异常，不允许无限自主扩展。

满足以下任一情况时，应停止无序尝试并重新评估：

1. 同一个错误连续出现两次；
2. 标准打包已经重试一次仍失败；
3. 下一步需要明显扩大任务范围；
4. 下一步需要修改构建架构；
5. 下一步可能影响用户已有程序或文件；
6. 下一步需要删除已有 release 或用户数据；
7. 已经无法证明新的尝试比之前更有针对性。

此时优先：

```text
总结已知事实
→ 明确根因或当前未知点
→ 给出最小风险下一步
```

而不是继续随机尝试。

---

## 16. 输出要求

最终回复应明确区分：

- 已完成；
- 已修改；
- 已测试；
- 已验证；
- 尚未验证；
- 当前阻塞问题；
- 推断与事实。

不要声称：

- 文件已经修改，如果实际没有修改；
- 测试通过，如果实际没有运行；
- 软件可运行，如果只完成 build；
- EXE 可用，如果没有生成；
- 远程仓库已同步，如果只检查本地状态。

输出应优先简洁、具体。

不要把简单任务的最终报告扩展成长篇项目审计。

---

## 17. 优先级

执行任务时按以下优先级处理：

1. **用户当前明确指令**
2. **本项目 `AGENTS.md`**
3. **当前真实工作区和文件状态**
4. **项目现有脚本、配置和工程惯例**
5. **最小风险、最小修改原则**
6. **历史 Memory**

Memory 只用于提供历史背景和经验。

对于以下内容，应以当前实际状态为准：

- 文件；
- 版本；
- branch；
- workspace；
- PATH；
- 软件版本；
- release；
- 当前构建产物；
- 当前运行进程。

---

## 18. 完成前最终检查

重要任务结束前只确认必要事项：

- 是否完成用户当前要求？
- 是否使用正确的当前文件和版本？
- 是否误用了过期 Memory？
- 是否超出用户授权范围？
- 是否保护用户已有修改？
- 必要验证是否已经通过？
- 是否正在重复执行已经通过的检查？
- 是否仍有 Codex 自己启动且应该清理的临时进程？
- 如果用户要求打包，目标 EXE 是否已经生成且版本正确？

如果任务已经满足 Definition of Done：

> **立即停止，不继续追加工具调用。**